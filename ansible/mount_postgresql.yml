---

- hosts: all

  pre_tasks:
    - mount: name="/var/lib/postgresql/" src={{fs_src}} fstype={{fs_type}} state=mounted
      sudo: yes

    - shell: "rm -rf /var/lib/postgresql/*"
      sudo: yes
      ignore_errors: yes
