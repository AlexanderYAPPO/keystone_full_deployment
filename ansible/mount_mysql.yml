---

- hosts: all

  pre_tasks:
    - mount: name={{mariadb_datadir}} src={{global_fs_src}} fstype={{global_fs_type}} state=mounted

    - shell: "rm -rf {{mariadb_datadir}}/*"
      sudo: yes

    - command: chown mysql:mysql {{mariadb_datadir}} -R
  vars:
      mariadb_datadir: /var/lib/mysql



